# SSQuant v0.4.6 更新日志

> 对比基准：当前根目录 v0.4.5 与 `046/` 目录
> 对比日期：2026-06-02
> 升级范围：`046/ssquant/`、`046/ai_agent/`
> 版本号：`ssquant.__version__` 从 `0.4.5` 更新为 `0.4.6`

---

## 一、核心结论

v0.4.6 不是一次接口大改，而是一次围绕 **复权连续合约回测真实性** 和 **AI Agent 稳定性** 的修复增强版本。

本版最重要的框架变化是：策略层仍可使用复权价格做指标和信号，但框架会通过价格双轨制把复权价映射回真实价格，并用真实价格计算回测指标、持仓盯市、权益曲线和报告统计，从而消除连续合约后复权/前复权在换月点造成的“复权幻觉”。

AI Agent 部分则从 Flask 开发服务器模式升级到 waitress 生产启动模式，并加强了思考模型、附件上传、运行进程管理、SSE 日志推送和工作区级联清理。

---

## 二、框架核心更新

### 1. 价格双轨制：复权价用于策略，映射真实价用于回测指标

v0.4.6 在 `DataSource` 中新增 `current_raw_price` 与交易记录字段 `raw_price`。

旧版本中，连续合约数据如果使用后复权或前复权，策略信号、成交价、持仓盯市和权益曲线都使用同一套复权价。这样做对技术指标连续性有利，但在换月点会让真实盈亏被复权因子污染。

新版本改为双轨：

| 价格口径 | 用途 |
|---|---|
| `price` / `current_price` | 策略可见价格、指标、K 线图、限价触发等 |
| `raw_price` / `current_raw_price` | 映射真实价格、回测指标、持仓盯市、权益曲线、统计报告 |

主要影响文件：

- `ssquant/data/data_source.py`
- `ssquant/backtest/backtest_core.py`
- `ssquant/backtest/backtest_results.py`
- `ssquant/backtest/html_report.py`

### 2. 复权因子 `_adjust_factor` 进入内存数据链路

`local_adjust.apply_local_adjust()` 现在会在返回的 DataFrame 中附加内部列 `_adjust_factor`。

规则如下：

- `adjust_type='0'` 或无换月：`_adjust_factor = 1.0`
- 后复权/前复权：OHLC 按因子调整，同时保留 `_adjust_factor`
- 回测指标计算时使用 `raw = adjusted / _adjust_factor` 映射真实价格

这使得策略继续看到连续平滑的复权 K 线，而账户层可以用原始合约价格计算真实 P&L。

### 3. 远程数据统一缓存 raw，出口处本地复权

`api_data_fetcher.get_futures_data()` 新增“复权重定向”逻辑：

- 远程 API 和 SQLite 缓存统一走不复权 raw 路径
- 不再读写旧的 `xxx_hfq` / `xxx_qfq` 复权缓存表
- 函数出口处根据用户请求的 `adjust_type` 调用 `apply_local_adjust()`
- Tick 数据不参与复权，仍按原逻辑处理

好处：

- 避免 raw、hfq、qfq 多套缓存互相污染
- 首次 API 拉取和后续缓存命中的返回口径更一致
- 为价格双轨制提供稳定的 `_adjust_factor`

### 4. 日期范围更严格，避免数据边界外溢

v0.4.6 增加了两层日期裁切：

1. `api_data_fetcher.py` 对 API 直取数据严格裁到 `[start_dt, end_dt]`
2. `backtest_core.py` 在多数据源 `align_data()` 后按所有数据源配置做最终保险截断

这修复了部分 API 或缓存边界多吐邻近交易日数据，导致首次运行和缓存命中运行结果不一致的问题。

另外，日期解析容错增强：类似 `2026-04-31` 这类无效日期会自动修正为该月有效日期，而不是简单失败。

### 5. 滑点按真实价格单位一致计算

复权场景下，滑点的真实单位应该仍是合约最小变动价位。

v0.4.6 在撮合时会将 `slippage_ticks * price_tick` 先按当前复权因子映射到复权价空间，再通过 `_unadjust_price()` 还原，使最终 raw 成交价中的滑点与不复权回测一致。

### 6. 综合最大回撤口径修正

`backtest_results.py`、`backtest_report.py`、`html_report.py` 都调整了综合最大回撤计算：

- 旧逻辑部分场景使用单品种平均或符号为负的回撤
- 新逻辑基于所有数据源合成后的综合权益曲线计算
- 回撤值统一为正数口径：`cummax - equity`

这会影响多品种、多周期回测报告中的最大回撤与最大回撤率。

### 7. CTP 平今/平昨偏移位更严格

`pyctp/trader_api.py` 中 `close_comb_offset_flag()` 不再在无法推导交易所时静默 fallback 到普通平仓 `'1'`。

新逻辑会直接抛出 `ValueError`，提示刷新合约缓存或补充本地品种前缀映射。

原因：SHFE/INE 品种对平今、平昨偏移位敏感，静默 fallback 可能导致真实交易指令错误。

---

## 三、报告与可视化更新

### 1. HTML 报告大幅增强

`html_report.py` 是 v0.4.6 中改动最大的框架文件之一。

主要变化：

- 引入 TradingView Lightweight Charts 用于 K 线/Tick 价格图
- K 线图支持交易标记、十字光标、tooltip、弹窗查看
- 新增 K 线信号全景图生成与下载
- 新增综合页，汇总所有数据源指标和交易记录
- 交易表格支持筛选、分页、盈利/亏损过滤
- 交易价格在复权价与映射真实价不一致时显示“实际价”
- 利润曲线区分净利润与未扣成本利润
- 指标卡片明确展示手续费、滑点成本、交易盈亏、净利润

### 2. Matplotlib 静态图逻辑重构

`backtest_visualization.py` 被重构为更聚焦的静态图生成工具：

- Logo 水印改为在图内右上角叠加，不再占用额外子图空间
- Logo 路径查找更稳健，支持包资源、项目路径和 glob 查找
- 提供 `visualize_backtest()`、`_generate_price_chart()` 等更直接的图表生成接口
- 被 HTML 报告用于生成“信号全景图”

---

## 四、AI Agent 更新

### 1. 后端入口从 `app.py` 调整为 `backend.py`

`046/ai_agent/backend.py` 由原 `ai_agent/app.py` 演进而来，但文件尾部明确说明不再建议直接 `python backend.py`。

新启动方式：

```powershell
cd ai_agent
python start_server.py
```

### 2. 新增 waitress 生产级启动脚本

新增文件：

- `ai_agent/start_server.py`

新依赖：

- `waitress>=2.1.0`

目的：

- 替代 Flask 开发服务器
- 改善 Windows 下长时间运行、后台切换、SSE 连接不稳定的问题
- 使用 8 线程处理 SSE 长连接和 API 请求

### 3. AI 调用增强：思考模式、重试、多模态

AI 设置新增：

- `thinking_mode`
- `max_reasoning_length`
- `max_tokens`

主要能力：

- 支持 DeepSeek-R1 等 OpenAI 兼容接口的 `reasoning_content`
- 支持 Claude 原生 API 的 thinking block
- Claude 开启思考模式时自动注入 `thinking.budget_tokens`
- 流式 API 失败时最多自动重试 5 次，指数退避
- 支持图片和文本附件上传
- Claude 消息转换支持多模态 content

### 4. 策略运行进程管理增强

回测执行逻辑从“只跑回测”扩展为识别运行模式：

- `RunMode.BACKTEST`
- `RunMode.SIMNOW`
- `RunMode.REAL_TRADING`

新能力：

- 自动检测策略代码中的 `RUN_MODE = RunMode.xxx`
- 根据模式显示“回测 / SIMNOW 模拟交易 / CTP 实盘交易”
- 子进程 stdout 改为写入日志文件，后台线程轮询读取，避免 Windows PIPE 阻塞
- 新增 `/api/backtest/stop`，Windows 下使用 `taskkill /F /T /PID` 强制终止进程树
- SSE 状态推送加入心跳、done 保护和调试日志

### 5. 工作区与报告清理增强

删除工作区时，v0.4.6 会级联清理：

- 关联 HTML 报告
- 关联 performance 文本报告
- 关联策略 `.py` 文件
- 策略历史记录
- `report_metadata.json` 中的报告映射

这能避免工作区删除后，旧报告和旧策略文件继续残留。

### 6. 前端页面大幅更新

`templates/index.html` 改动较大，配合后端新增能力：

- 思考内容展示
- 附件上传
- 自动运行/自动调试/自动迭代控制
- 运行状态和强制停止
- 报告列表和工作区关联
- 更完整的 Monaco 编辑器交互

---

## 五、升级注意事项

### 1. 回测结果可能与 v0.4.5 不一致，这是预期变化

只要策略使用了连续合约复权数据，v0.4.6 的盈亏、权益曲线、最大回撤、滑点统计都可能与 v0.4.5 不同。

这不是兼容性 bug，而是 v0.4.6 改为用价格双轨制映射真实价格计算回测指标后的结果修正。

### 2. 旧的复权缓存表不再作为主路径

磁盘上已有的 `hfq/qfq` 缓存表不会被自动删除，但 v0.4.6 的主逻辑不再读写这些表。

建议后续数据维护统一围绕 raw 表和本地复权逻辑展开。

### 3. 交易记录新增 `raw_price`

旧报告或旧交易记录没有 `raw_price` 时，新代码会回退使用 `price`，因此保持向后兼容。

但新报告中，如果复权价与真实价不同，会同时显示复权价和“实际价”，便于对账。

### 4. CTP 合约交易所推导失败会显式报错

如果实盘或 SIMNOW 下某个新品种无法从远程合约缓存或本地前缀表推导交易所，v0.4.6 会抛错。

升级后需要验证：

- `contract_info` 缓存是否最新
- 新品种前缀是否已写入 `_get_exchange_id()` 的本地映射

### 5. AI Agent 覆盖时要保护运行产物

`046/ai_agent/` 中包含较多运行快照：

- `strategies/`：261 个历史策略文件
- `workspaces/`：5 个工作区文件
- `history.json`
- `settings.json`
- `report_metadata.json`

这些更像本次打包时带入的运行产物，不一定应该直接覆盖用户当前环境。

建议正式替换时：

- 覆盖代码文件：`backend.py`、`start_server.py`、`templates/index.html`、`README.md`、`requirements.txt`
- 谨慎处理运行数据：`strategies/`、`workspaces/`、`history.json`、`settings.json`、`report_metadata.json`
- 如果当前环境已有真实工作区，先备份再合并

---

## 六、主要变更文件清单

### SSQuant 框架

| 文件 | 主要变化 |
|---|---|
| `ssquant/__init__.py` | 版本号更新为 `0.4.6` |
| `ssquant/data/local_adjust.py` | 新增 `_adjust_factor`，支持复权价还原 raw 价 |
| `ssquant/data/api_data_fetcher.py` | 远程数据统一 raw 缓存，出口本地复权；日期裁切增强 |
| `ssquant/data/data_source.py` | 新增 `current_raw_price`、`raw_price`、价格双轨映射逻辑 |
| `ssquant/backtest/backtest_core.py` | 主循环设置 raw 价；综合账户快照使用 raw 价盯市 |
| `ssquant/backtest/backtest_results.py` | 权益、盈亏、持仓盯市使用 raw 价；综合回撤改为综合权益曲线 |
| `ssquant/backtest/backtest_report.py` | 最大回撤正数口径修正 |
| `ssquant/backtest/backtest_visualization.py` | 静态图和 Logo 水印逻辑重构 |
| `ssquant/backtest/html_report.py` | HTML 报告、K 线交互图、综合页、交易表格大幅增强 |
| `ssquant/pyctp/trader_api.py` | 平今/平昨交易所推导失败时显式报错 |

### AI Agent

| 文件 | 主要变化 |
|---|---|
| `ai_agent/backend.py` | 原 `app.py` 演进为新后端入口，支持思考模式、附件、重试、进程停止 |
| `ai_agent/start_server.py` | 新增 waitress 生产启动脚本 |
| `ai_agent/templates/index.html` | 前端功能大幅增强 |
| `ai_agent/requirements.txt` | 新增 waitress 依赖，启动方式改为 `python start_server.py` |
| `ai_agent/README.md` | 更新为新架构说明 |
| `ai_agent/report_metadata.json` | 新增报告与工作区关联映射 |

---

## 七、建议验证清单

正式替换前建议至少做以下验证：

1. 不复权回测：确认 v0.4.6 与 v0.4.5 结果基本一致。
2. 后复权连续合约回测：确认交易记录同时有 `price` 与 `raw_price`，报告指标按映射真实价计算。
3. 多品种多周期回测：确认 `align_data` 后日期范围没有超出配置。
4. HTML 报告：确认 K 线图、交易标记、综合页、交易表格、信号全景图正常显示。
5. 参数优化：确认静默模式下不生成多余报告或阻塞。
6. SIMNOW/实盘：确认 SHFE/INE 品种平今/平昨偏移位正常，未知品种会明确报错。
7. AI Agent：用 `python start_server.py` 启动，确认聊天 SSE、运行日志 SSE、强制停止、工作区删除正常。

---

## 八、替换建议

建议 v0.4.6 采用分阶段替换：

1. 先替换 `ssquant/`，运行基础回测和复权回测对账。
2. 再替换 AI Agent 代码文件，但保留当前用户的 `settings.json`、`history.json`、`workspaces/` 和 `strategies/`。
3. 最后根据需要合并 `046/ai_agent/` 中的示例策略与工作区。

本版最值得优先验证的是复权连续合约场景。只要这部分通过，v0.4.6 对专业期货回测的可信度会明显高于 v0.4.5。
